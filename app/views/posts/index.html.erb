<div class="d-flex align-items-center justify-content-between mb-3">
  <h1 class="display-5 text-light">Noticias 😃</h1>
  <% if user_signed_in? %>
    <%= link_to "Nueva Noticia", new_post_path, class: "btn btn-outline-light" %>
  <% end %>
</div>
<div id="posts">
  <% @posts.each do |post| %>
    <div class="mb-5 border p-3 rounded-4">
      <%= render post %>
      <p>
        <%= link_to "Ver mas..", post, class: "link-light" %>
      </p>
    </div>
  <% end %>
</div>
